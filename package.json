{
  "name": "eclipso",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "generate:ts": "openapi-generator-cli generate -i http://localhost:3000/api-json -g typescript-fetch -o packages/api-client-ts --global-property=models --additional-properties=api=false && node ./scripts/clean-runtime-import.mjs"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.25.0",
    "prettier": "^3.6.2",
    "turbo": "^2.6.0",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
